spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://3.38.162.136:3306/threetier?useSSL=false&serverTimezone=Asia/Seoul&allowPublicKeyRetrieval=true
    username: threetier
    password: 1234


server:
  port: 10000


  servlet:
    multipart:
      enabled: true
      file-size-threshold: 10MB
      max-file-size: 5MB
      max-request-size: 15MB

  mail:
    host: smtp.gmail.com
    port: 587
    username: tedhan.temp
    password: nspd mhxc erbw qfjl
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
#            auth, 사용자 인증(username, password 확인)
#            start tls(Transport Layer Security), 메일 내용이 네트워크상에서 암호화되어 전송

mybatis:
  configuration:
    map-underscore-to-camel-case: true
  mapper-locations: classpath*:/mapper/*.xml
  type-aliases-package: com.app.threetier.domain; com.app.threetier.dto;
  type-handlers-package: com.app.threetier.mybatis.handler



